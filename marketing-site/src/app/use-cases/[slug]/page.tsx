import Link from "next/link";
import { Container } from "@/components/ui/Container";
import { USE_CASES } from "@/lib/authority-assets";
import { SITE_URL } from "@/lib/site";

export function generateStaticParams() { return USE_CASES.map((item) => ({ slug: item.slug })); }
export async function generateMetadata({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const item = USE_CASES.find((entry) => entry.slug === slug); return { title: `${item?.title ?? "Salon Use Case"} — Aura Salon CRM/POS`, description: item?.focus, alternates: { canonical: `${SITE_URL}/use-cases/${slug}` } }; }
export default async function UseCasePage({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const item = USE_CASES.find((entry) => entry.slug === slug) ?? USE_CASES[0]; const faq = { "@context": "https://schema.org", "@type": "FAQPage", mainEntity: [{ "@type": "Question", name: `How does Aura help with ${item.title}?`, acceptedAnswer: { "@type": "Answer", text: item.focus } }] }; return <><script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(faq) }} /><section className="bg-[var(--aura-off-white)] py-28"><Container size="narrow"><p className="text-xs font-bold uppercase tracking-[.16em] text-[var(--aura-purple)]">Use case</p><h1 className="mt-4 text-4xl font-bold tracking-[-0.04em]">{item.title}</h1><p className="mt-5 text-lg leading-8 text-[var(--aura-body)]">{item.focus}</p><div className="mt-8 grid gap-4"><article className="rounded-3xl bg-white p-6"><h2 className="font-bold">Workflow</h2><p className="mt-2 text-[var(--aura-body)]">Map the current process, define one rule, connect it to Aura, then review the metric weekly.</p></article><article className="rounded-3xl bg-white p-6"><h2 className="font-bold">Related Aura pages</h2><div className="mt-3 flex flex-wrap gap-2"><Link href="/features" className="rounded-full border px-3 py-1 text-xs font-semibold">Features</Link><Link href="/demo" className="rounded-full border px-3 py-1 text-xs font-semibold">Demo</Link><Link href="/calculators" className="rounded-full border px-3 py-1 text-xs font-semibold">Calculators</Link></div></article></div></Container></section></>; }
