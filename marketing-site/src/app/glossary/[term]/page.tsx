import { Container } from "@/components/ui/Container";
import { GLOSSARY_TERMS } from "@/lib/authority-assets";

export function generateStaticParams() { return GLOSSARY_TERMS.map((item) => ({ term: item.slug })); }
export default async function GlossaryTermPage({ params }: { params: Promise<{ term: string }> }) { const { term } = await params; const item = GLOSSARY_TERMS.find((entry) => entry.slug === term) ?? GLOSSARY_TERMS[0]; const jsonLd = { "@context": "https://schema.org", "@type": "DefinedTerm", name: item.term, description: item.definition }; return <><script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(jsonLd) }} /><section className="bg-[var(--aura-off-white)] py-28"><Container size="narrow"><p className="text-xs font-bold uppercase tracking-[.16em] text-[var(--aura-purple)]">Glossary</p><h1 className="mt-4 text-4xl font-bold tracking-[-0.04em]">{item.term}</h1><p className="mt-5 text-lg leading-8 text-[var(--aura-body)]">{item.definition}</p><div className="mt-8 rounded-3xl bg-white p-6"><h2 className="font-bold">Why it matters in Aura</h2><p className="mt-2 text-[var(--aura-body)]">This concept becomes more useful when it connects to booking, billing, CRM, staff, inventory and reporting workflows.</p></div></Container></section></>; }
