import { Container } from "@/components/ui/Container";
import { INTEGRATIONS } from "@/lib/authority-assets";
import { SITE_URL } from "@/lib/site";

export function generateStaticParams() { return INTEGRATIONS.map((item) => ({ slug: item.slug })); }
export async function generateMetadata({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const item = INTEGRATIONS.find((entry) => entry.slug === slug); return { title: `${item?.name ?? "Integration"} Integration for Salons — Aura`, description: `How Aura works with ${item?.name ?? "this integration"} workflows for salon booking, billing, CRM and operations.`, alternates: { canonical: `${SITE_URL}/integrations/${slug}` } }; }
export default async function IntegrationPage({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const item = INTEGRATIONS.find((entry) => entry.slug === slug) ?? INTEGRATIONS[0]; return <section className="bg-[var(--aura-off-white)] py-28"><Container size="narrow"><p className="text-xs font-bold uppercase tracking-[.16em] text-[var(--aura-purple)]">Integration</p><h1 className="mt-4 text-4xl font-bold tracking-[-0.04em]">{item.name} for salon workflows</h1><p className="mt-5 text-lg leading-8 text-[var(--aura-body)]">Aura supports {item.name} as a {item.status} workflow. Confirm exact setup during demo based on your branch, payment and communication needs.</p><div className="mt-8 rounded-3xl bg-white p-6"><h2 className="font-bold">Common workflow</h2><p className="mt-2 text-[var(--aura-body)]">Connect this workflow with booking, POS, CRM, staff, inventory or reporting depending on your salon process.</p></div></Container></section>; }
